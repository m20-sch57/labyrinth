class Data

В начале работы он загружает из базы данных всё нужное, остальные части программы работают с ним.

Методы:

get_user_profile(id)
По идентификатору пользователя возвращает данные его профиля

get_map(id)
По идентификатору карты возвращает её

get_room(id)
По идентификатору комнаты возвращает её
